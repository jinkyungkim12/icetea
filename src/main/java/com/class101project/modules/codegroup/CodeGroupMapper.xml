<?xml version="1.0" encoding="UTF-8" ?>

<!--2. mapper .xml -->

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.class101project.modules.codegroup.CodeGroupMapper">
<!-- code -> codegroup -->
    <resultMap id="resultMapObj" type="com.class101project.modules.codegroup.CodeGroup"></resultMap>
	
	<!-- <sql id="selectCommon">
		FROM codeGroup a
		WHERE 1=1
			<if test="shDelNy ! =null and ! shDelNy.equals('')">AND delNY = #{shDelNy}</if>
			<choose>
				<when test="shOption == 1">AND seq =#{shValue}</when>
				<when test=
			</choose>
	</sql>
	 -->
	<select id="selectList" resultMap="resultMapObj">
		SELECT
		 a.seq
           ,a.codeGroupCode
           ,a.nameKr
           ,a.name
           ,(select count(b.codeGroup_seq) from code b where b.delNY=0 and b.codeGroup_seq=a.seq)as codeNum
           ,a.delNY
           ,a.useNY
	FROM codeGroup a
	WHERE 1=1
		AND delNY = 0
		AND name LIKE CONCAT('%', #{shValue}, '%')	
			<!-- SELECT 
				a.*
			FROM chooseTest a
			WHERE 1=1
				AND delNY = 0 -->
	
	</select>

</mapper>	