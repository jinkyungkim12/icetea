<?xml version="1.0" encoding="UTF-8" ?>

<!--2. mapper .xml -->

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.class101project.modules.code.CodeMapper">
<!-- code -> codegroup -->
    <resultMap id="resultMapObj" type="com.class101project.modules.code.Code"></resultMap>
	
	<select id="selectList" resultMap="resultMapObj">
		SELECT
			a.seq
			,a.codeGroup_seq
			,b.nameKr
			,a.code
			,a.codeAnother
			,a.codeNameKr
			,a.codeName
			,a.codeUseNY
			,a.codeDelNY
			,a.order
			,a.codeRegDate
			,a.codeModDate
		from code a 
		left join codeGroup b on b.seq = a.codeGroup_seq
		 WHERE 1=1
		
		<if test="shCodeDelNY !=null and !shCodeDelNY.equals('')">AND a.codeDelNY = #{shCodeDelNY}</if>
		<if test="shValue != null"></if>
		
		
		<choose>
			<when test="shOption == 1">AND a.codeGroup_seq = #{shValue}</when>
			<when test="shOption == 2">AND b.nameKr LIKE CONCAT('%', #{shValue},'%')</when>
			<when test="shOption == 3">AND a.codeNameKr LIKE CONCAT('%', #{shValue},'%')</when>
			<when test="shOption == 4">AND a.codeName LIKE CONCAT('%', #{shValue},'%')</when>
		</choose>
		
		<choose>
			<when test="shOptionDate == 5">AND a.codeRegDate BETWEEN #{shDateStart} AND #{shDateEnd}</when>
			<when test="shOptionDate == 6">AND a.codeModDate BETWEEN #{shDateStart} AND #{shDateEnd}</when>
		</choose>
		
	</select>

	<insert id="insert">
		INSERT INTO code(
			codeGroup_seq
			,codeNameKr
			,codeName
			,codeUseNY
			,codeDelNY
			,codeRegDate
		)
		VALUES (
			 #{codeGroup_seq}
			,#{codeNameKr}
			,#{codeName}
			,1
			,0
			,now()
		)
		<selectKey resultType="String" keyProperty="seq" order="AFTER">
			SELECT last_insert_id()
		</selectKey>
	</insert>
	
</mapper>