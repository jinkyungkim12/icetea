<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.class101project.modules.member.MemberMapper">
<!-- code -> codegroup -->
    <resultMap id="resultMapObj" type="com.class101project.modules.member.Member"></resultMap>
	
	<select id="selectList" resultMap="resultMapObj">
		select a.seq
			  ,a.name
		      ,a.id
		      ,a.position
		      ,a.gender
		      ,a.dob
		      ,a.phone
		      ,a.email
		      ,a.mobileNY
		      ,a.emailNY
		      ,a.personalInfo
		      ,a.delNY
		      ,a.regDatetime
		FROM member a
		WHERE 1=1
			<!-- AND delNY = 0
			AND id LIKE CONCAT('%', #{shValue}, '%') -->
		<!-- select a.*
		FROM member a
		WHERE 1=1
			AND delNY = 0 -->
			
		<if test="shdelNY !=null and !shdelNY.equals('')">AND a.delNY = #{shdelNY}</if>
		<if test="shValue != null"></if>
		
		<choose>
			<when test="shOption == 1">AND a.name LIKE CONCAT('%', #{shValue},'%')</when>
			<when test="shOption == 2">AND a.id LIKE CONCAT('%', #{shValue},'%')</when>
			<when test="shOption == 3">AND a.gender LIKE CONCAT('%', #{shValue},'%')</when>
			<when test="shOption == 4">AND a.email LIKE CONCAT('%', #{shValue},'%')</when>
			<when test="shOption == 5">AND a.position LIKE CONCAT('%', #{shValue},'%')</when>
		</choose>
		
		<choose>
			<when test="shOptionDate == 6">AND a.dob BETWEEN #{shDateStart} AND #{shDateEnd}</when>
			<when test="shOptionDate == 7">AND a.regDatetime BETWEEN #{shDateStart} AND #{shDateEnd}</when>
		</choose>
		
	</select>
	
	<!-- <insert id="insert">
		INSERT INTO member(
			name
			,id
			,position
			,gender
			,dob
			,phone
			,phone2
			,emailInsert
			,mobileNY
			,emailNY
			,personalInfo
			,delNY
			,regDatetime
		)
		VALUES (
			#{name}
			,#{nameKr}
			,#{id}
			,#{position}
			,#{gender}
			,#{dob}
			,#{phone}
			,#{phone2}
			,#{emailInsert}
			,#{mobileNY}
			,#{emailNY}
			,#{personalInfo}
			,0
			,now()
		)
		<selectKey resultType="String" keyProperty="seq" order="AFTER">
			SELECT last_insert_id()
		</selectKey>
	</insert> -->

</mapper>